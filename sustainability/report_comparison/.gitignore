.next/
node_modules/
out/
.env*.local
.vercel

# TypeScript incremental build info
tsconfig.tsbuildinfo

# --- Built Next.js static export (regenerated by the root Vercel buildCommand — DO NOT COMMIT) ---
# These are copied in from ../esg-export at build time; source of truth is src/.
# A stale committed export here silently 404s new routes (missing dir) or breaks assets
# (buildId mismatch). Add any new route's built dir below.
/_next/
/index.html
/index.txt
/404.html
/404/
/banks/
/infra/
/healthcare/
