{
  "name": "priscilla-site",
  "version": "1.0.0",
  "scripts": {
    "dev": "tinacms dev -c \"npx @11ty/eleventy --serve\"",
    "build": "npx @11ty/eleventy && (tinacms build || echo 'Tina build skipped')"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@anthropic-ai/sdk": "^0.52.0",
    "@tinacms/cli": "^2.5.1",
    "@upstash/redis": "^1.38.0",
    "@vercel/blob": "^2.6.1",
    "@vercel/og": "^0.6.4",
    "ioredis": "^5.4.1",
    "tinacms": "^3.9.3"
  }
}
