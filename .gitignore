node_modules/
_site/
public/
blog/
.tina/__generated__/
.vercel
.DS_Store
**/.DS_Store
.posthog.json
tina/__generated__/

# ESG tracker static-export output (build artifact)
sustainability/esg-export/

# Source sustainability reports. Deposited here locally and read by
# scripts/extract; NEVER committed — they are hundreds of MB of binary, and
# the point of data/extracted/*.json is that the artifact replaces the PDF.
sustainability/sustainability-report/
.env*
